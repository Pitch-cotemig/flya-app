name: 🔍 Verificar Pull Request

on:
  pull_request:
    types: [opened, edited, synchronize, reopened]

jobs:
  validate-pr:
    runs-on: ubuntu-latest
    steps:
      - name: 🔎 Verificar título da PR
        uses: actions/github-script@v7
        with:
          script: |
            const prTitle = context.payload.pull_request.title;
            const titleRegex = /^\[FLYA\] - .+/;

            if (!titleRegex.test(prTitle)) {
              core.setFailed("❌ O título da PR deve começar com '[FLYA] - ' seguido de uma descrição.");
            } else {
              console.log("✅ Título válido");
            }

      - name: 📝 Verificar se há descrição na PR
        uses: actions/github-script@v7
        with:
          script: |
            const body = context.payload.pull_request.body || "";

            const match = body.match(/###\s*✍️\s*\*\*Descrição\*\*[\r\n]+([\s\S]*?)(?:\n|\r\n)?-{3,}/);

            if (!match || !match[1]) {
              core.setFailed("❌ Não foi possível encontrar a seção de descrição entre os delimitadores.");
            } else {
              const description = match[1]
                .replace(/<!--.*?-->/gs, "") // remove comentários
                .trim();

              console.log("📋 Descrição capturada:", description);

              if (!description || description.length < 10) {
                core.setFailed("❌ A descrição está vazia ou muito curta.");
              } else {
                console.log("✅ Descrição válida");
              }
            }

      - name: ☑️ Verificar checklist marcado
        uses: actions/github-script@v7
        with:
          script: |
            const body = context.payload.pull_request.body || "";
            const checklistItems = [
              "- [x] me atribuí como responsável por esta pr",
              "- [x] resolvi conflitos de merge (se houver)",
              "- [x] a pr está seguindo o padrão de nomenclatura do título",
              "- [x] atualizei/adicionei testes (opcional, mas melhor pra garantir estabilidade no futuro e garantir um bom coverage)",
              "- [x] revisei se há prints ou códigos de debug para remover"
            ];

            const bodyLower = body.toLowerCase();
            const allChecked = checklistItems.every(item => bodyLower.includes(item));
            if (!allChecked) {
              core.setFailed("❌ Nem todos os itens obrigatórios do checklist estão marcados.");
            } else {
              console.log("✅ Checklist completo");
            }
